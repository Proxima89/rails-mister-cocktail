<h1> Update a cocktail </h1>

<%= simple_form_for [@cocktail] do |f| %>
        <%= f.input :name %>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
      <%= link_to "See all cocktails", cocktails_path %>
      <br>
      <%= link_to "Add at dose", new_cocktail_dose_path(@cocktail) %>
      